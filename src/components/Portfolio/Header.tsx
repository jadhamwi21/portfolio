import React from "react";
import styled from "styled-components";

const Header = () => {
	return <Element id="portfolio">Portfolio</Element>;
};
const Element = styled.h1`
	text-align: center;
	color: var(--Yellow);
`;

export default Header;
