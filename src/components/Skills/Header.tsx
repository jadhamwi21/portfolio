import React from "react";
import styled from "styled-components";

const Header = () => {
	return <HeaderElement id="skills">Skills</HeaderElement>;
};
export const HeaderElement = styled.h1`
	text-align: center;
	color: var(--Yellow);
`;

export default Header;
